# Remote Server.
```
1) Find IP remote server;
2) Ping ip remote server, if ping - yes, 
a) check availability of port 22, if port is open, connect to ssh - ok. Download putty(for example) and connect to server
b) If port 22 is not available, you need to set up port forwarding and Download putty(for example) and connect to server
```
